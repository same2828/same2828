# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:/usr/local/bin:$PATH

# Path to your oh-my-zsh installation.
export ZSH="$HOME/.oh-my-zsh"

# Set name of the theme to load --- if set to "random", it will
# load a random theme each time oh-my-zsh is loaded, in which case,
# to know which specific one was loaded, run: echo $RANDOM_THEME
# See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes
ZSH_THEME="robbyrussell"

# Set list of themes to pick from when loading at random
# Setting this variable when ZSH_THEME=random will cause zsh to load
# a theme from this variable instead of looking in $ZSH/themes/
# If set to an empty array, this variable will have no effect.
# ZSH_THEME_RANDOM_CANDIDATES=( "robbyrussell" "agnoster" )

# Uncomment the following line to use case-sensitive completion.
# CASE_SENSITIVE="true"

# Uncomment the following line to use hyphen-insensitive completion.
# Case-sensitive completion must be off. _ and - will be interchangeable.
# HYPHEN_INSENSITIVE="true"

# Uncomment one of the following lines to change the auto-update behavior
# zstyle ':omz:update' mode disabled  # disable automatic updates
zstyle ':omz:update' mode auto      # update automatically without asking
# zstyle ':omz:update' mode reminder  # just remind me to update when it's time

# Uncomment the following line to change how often to auto-update (in days).
# zstyle ':omz:update' frequency 13

# Uncomment the following line if pasting URLs and other text is messed up.
# DISABLE_MAGIC_FUNCTIONS="true"

# Uncomment the following line to disable colors in ls.
# DISABLE_LS_COLORS="true"

# Uncomment the following line to disable auto-setting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment the following line to enable command auto-correction.
# ENABLE_CORRECTION="true"

# Uncomment the following line to display red dots whilst waiting for completion.
# You can also set it to another string to have that shown instead of the default red dots.
# e.g. COMPLETION_WAITING_DOTS="%F{yellow}waiting...%f"
# Caution: this setting can cause issues with multiline prompts in zsh < 5.7.1 (see #5765)
# COMPLETION_WAITING_DOTS="true"

# Uncomment the following line if you want to disable marking untracked files
# under VCS as dirty. This makes repository status check for large repositories
# much, much faster.
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# Uncomment the following line if you want to change the command execution time
# stamp shown in the history command output.
# You can set one of the optional three formats:
# "mm/dd/yyyy"|"dd.mm.yyyy"|"yyyy-mm-dd"
# or set a custom format using the strftime function format specifications,
# see 'man strftime' for details.
# HIST_STAMPS="mm/dd/yyyy"

# Would you like to use another custom folder than $ZSH/custom?
# ZSH_CUSTOM=/path/to/new-custom-folder

# Silence launching ssh-agent
zstyle :omz:plugins:ssh-agent quiet yes
zstyle ':fzf-tab:*' use-fzf-default-opts yes

# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(git ssh-agent fzf-tab zsh-autosuggestions fast-syntax-highlighting fzf)
# plugins=(git ssh-agent fzf-tab zsh-autosuggestions zsh-syntax-highlighting fzf)
# plugins=(git ssh-agent fzf-tab sudo dirhistory zsh-autosuggestions zsh-syntax-highlighting fzf)

# zsh-completions
# fpath+=${ZSH_CUSTOM:-${ZSH:-~/.oh-my-zsh}/custom}/plugins/zsh-completions/src

source $ZSH/oh-my-zsh.sh
# source <(fzf --zsh)

# User configuration

# export MANPATH="/usr/local/man:$MANPATH"

# You may need to manually set your language environment
# export LANG=en_US.UTF-8

# Preferred editor for local and remote sessions
# if [[ -n $SSH_CONNECTION ]]; then
#   export EDITOR='vim'
# else
#   export EDITOR='mvim'
# fi

# Compilation flags
# export ARCHFLAGS="-arch x86_64"

# Set personal aliases, overriding those provided by oh-my-zsh libs,
# plugins, and themes. Aliases can be placed here, though oh-my-zsh
# users are encouraged to define aliases within the ZSH_CUSTOM folder.
# For a full list of active aliases, run `alias`.
#
# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"

# Coreutils
#----------
# export PATH="/opt/homebrew/opt/coreutils/libexec/gnubin:$PATH"

# CSE SSH
#--------
# eval "$(ssh-agent -s)" &> /dev/null
# ssh-add ~/.ssh/gitlab-cse &> /dev/null

# Hadoop
#-------
# export HADOOP_HOME=/home/same/hadoop-3.3.2
# export HADOOP_CONF_DIR=$HADOOP_HOME/etc/hadoop
# export PATH=$HADOOP_HOME/bin:$HADOOP_HOME/sbin:$PATH
# export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64

# zsh-autosuggestions
#--------------------
# ctrl + j = toggle zsh-autosuggestions on/off
bindkey '^j' autosuggest-toggle
bindkey '^k' toggle-fzf-tab
# Enforce < 81 character limit
ZSH_AUTOSUGGEST_HISTORY_IGNORE="(?(#c81,)|*gcmsg '*|*gcmsg \"*|*gc!*|*gcmsg!*|(brew (install|uninstall|info|tap)|cat|cd|codium|cude|curl|echo|fetch|git (add|branch|checkout|clone|merge|rebase|remote)|ga|gc|gca|gcam|gcas|gcasm|gcb|gcl|gcmsg|gco|gcs|gcsm|git commit -(am|m)|mkdir|mv|npm (install|uninstall|link|unlink)|npx|pnpm (add|remove|install|uninstall|link|unlink|up|update|upgrade)|sh|wget|yarn (add|remove|link|unlink|up)|youtube-dl) *)"

# Homebrew
#---------
# eval $(/home/linuxbrew/.linuxbrew/bin/brew shellenv)
# export BREW_HOME="/home/linuxbrew/.linuxbrew/bin"
# export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"
# export PATH="/usr/local/sbin:$PATH" # macOS Intel (could also be /usr/local/bin)
export PATH="/opt/homebrew/bin:$PATH" # macOS M1

# Starship
#---------
eval "$(starship init zsh)"

# VSCode
#-------
# export PATH=$PATH:"/mnt/c/Users/same/AppData/Local/Programs/Microsoft VS Code/bin"
export PATH="$PATH:/Applications/Visual Studio Code.app/Contents/Resources/app/bin"

# FNM
#----
# export PATH="$HOME/.fnm:$PATH" # Only needed if fnm installed via CLI and NOT with Homebrew
eval "$(fnm env --use-on-cd --shell zsh)"
function fnm() {
  # Extends fnm with the subcommand "upgrade"
  # https://github.com/Schniz/fnm/issues/781#issuecomment-2482846772
  if [[ $1 == "upgrade" ]]; then
    local matched_versions=()
    local pattern_match_version="* v$2"
    local pattern_match_system="* system $2"
    local is_system=0
    local line=""
    command fnm ls | while read LINE; do
      line=${LINE}
      if (( $line[(I)$pattern_match_version] )); then
        matched_versions+=${${${line#* v}%% *}%% default}
      fi
      if (( $line[(I)$pattern_match_system] )); then
        is_system=1
      fi
    done
    for version in $matched_versions; do
      command fnm uninstall $version
    done
    command fnm install "$2"
    if (( $is_system )); then
      echo "Re-alias system default version"
      command fnm alias system $2
    fi
  else
    command fnm "$@"
  fi
}

# NVM
#----
# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
# [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

# NodeJS
#-------
# export PATH="$PATH:/home/linuxbrew/.linuxbrew/opt/node@22/bin"
# export PATH="$PATH:/usr/local/opt/node@22/bin"
# export PATH="$PATH:/opt/homebrew/opt/node@22/bin"

# FZF
#----
# alias f="fzf"
# export FZF_BASE="/opt/homebrew/bin/fzf"
# Non FD
# export FZF_DEFAULT_COMMAND="--walker-skip .git,node_modules,target"
# export FZF_CTRL_T_COMMAND="--walker-skip .git,node_modules,target"
# export FZF_CTRL_R_COMMAND="--walker-skip .git,node_modules,target"
# export FZF_ALT_C_COMMAND="--walker-skip .git,node_modules,target --preview 'tree -C {}'"
# export FZF_ALT_C_OPTS="--walker-skip .git,node_modules,target --preview 'tree -C {}'"
# Using FD
# export FZF_DEFAULT_COMMAND="fd --type f --strip-cwd-prefix --hidden --follow --exclude={'**/.DS_Store/**','**/.dump*/**','**/.git/**','**/.idea/**','**/.npm*/**','**/.nvm/**','**/.pnpm*/**','**/.temp*/**','**/.tmp*/**','**/.vol/**','**/.vscode*/**','**/.yarn*/**','**/Applications/**','**/bin/**','**/build/**','**/cores/**','**/dist/**','**/downloads/**','**/eggs/**','**/etc/**','**/history/**','**/lib/**','**/lib64/**','**/Library/**','**/mnt/**','**/node_modules/**','**/opt/**','**/parts/**','**/private/**','**/sbin/**','**/sdist/**','**/System/**','**/target/**','**/temp/**','**/tmp/**','**/usr/**','**/var/**','**/venv/**','**/Volumes/**','**/wheels/**'}"
FD_OPTIONS="--strip-cwd-prefix --follow --exclude={'**/.*OneDrive*','**/.android','**/.aws','**/.azure','**/.backup*','**/.bin*','**/.boto','**/.brew','**/.build','**/.bundle','**/.cache*','**/.cfg','**/.CFUserTextEncoding*','**/.classpath','**/.codeium','**/.conf','**/.config','**/.cups','**/.dbus','**/.dist*','**/.docker','**/.dotnet','**/.DS_Store','**/.dump*','**/.editorconfig','**/.eggs','**/.env','**/.factorypath','**/.firebase','**/.font*','**/.git','**/.gitflow*','**/.gradle','**/.hg*','**/.history','**/.homebrew','**/.hushlogin','**/.icloud','**/.idea*','**/.kube/cache','**/.landscape','**/.lemminx','**/.lesshst','**/.linuxbrew','**/.local','**/.localized','**/.m2','**/.matplotlib','**/.mounty','**/.next','**/.node_modules*','**/.node*','**/.nox','**/.npm*','**/.nvm*','**/.oh-my-zsh','**/.out','**/.output','**/.pnpm*','**/.postman','**/.project','**/.python*','**/.settings','**/.sf','**/.sts*','**/.svn','**/.target','**/.temp*','**/.temporary*','**/.temurin*','**/.tldrc','**/.tmp*','**/.tox','**/.trash*','**/.Trash*','**/.venv','**/.vercel','**/.vnc','**/.vol','**/.vscode*','**/.webpack*','**/.yarn*','**/.zcompdump*','**/.zprofile?*','**/.zsh_sessions*','**/.zshrc?*','**/*OneDrive*','**/3D Objects','**/AppData','**/Application*','**/backup*','**/Backup*','**/bin','**/boot','**/build.*','**/build*','**/Build*','**/cache*','**/Cache*','**/certs','**/Certs','**/Chrome Download','**/Chrome*','**/Contacts','**/cores','**/cvmfs','**/CVS','**/Default','**/Desktop','**/dist','**/Documents','**/Drop*Box','**/dump*','**/eggs','**/env','**/etc','**/External*','**/favorite*','**/Favorite*','**/favourite*','**/Favourite*','**/font*','**/Font*','**/Game*','**/history*','**/History*','**/home/(!s3same)','**/home/(!s3same)','**/home/(!s3samedev)','**/home/(!same)','**/htmlcov','**/Idm','**/IDM','**/image*','**/Image*','**/img*','**/InstallArea','**/Intel','**/Lecture*','**/lib','**/Lib','**/lib64','**/Libraries','**/Library*','**/Link*','**/media','**/Media','**/mnt','**/Movies','**/Mp3tag','**/Music','**/node_modules*','**/NVIDIA*','**/opt','**/out','**/output','**/People','**/PerfLogs','**/Personal','**/photo*','**/Photo*','**/pic*','**/Pic*','**/picture*','**/Picture*','**/Postman','**/prebundle','**/private','**/proc','**/Program Files (x86)','**/Program Files','**/ProgramData','**/Public','**/root','**/Root','**/run','**/Saved Games','**/sbin','**/Screenshot*','**/sdist','**/Search*','**/Shared*','**/Sharing*','**/srv','**/Standard Notes*','**/standardnotes*','**/Steam','**/sys','**/System*','**/target.*','**/target*','**/Target*','**/temp*','**/Temp*','**/Thumbs.db','**/tmp*','**/Tmp*','**/Tool*','**/torrent*','**/Torrent*','**/usr','**/Utilities','**/Utility','**/var','**/venv','**/vid*','**/Vid*','**/video*','**/Video*','**/vod*','**/Vod*','**/volumes','**/Volumes','**/vscode*','**/wheels','**/Windows'}"
export FZF_DEFAULT_COMMAND="fd --type f --type l --hidden $FD_OPTIONS"
export FZF_CTRL_T_COMMAND="fd --hidden $FD_OPTIONS"
export FZF_ALT_C_COMMAND="fd --type d $FD_OPTIONS"
export FZF_DEFAULT_OPTS='
  --border
  --color fg:15,bg:0,hl:215,fg+:15,bg+:239,hl+:10
  --color info:141,prompt:10,spinner:212,pointer:212,marker:212
  --height=50%
  --layout=reverse
'

# Java
#-----
# alias j='f(){ export JAVA_HOME=/usr/libexec/java_home -v $1 }; java -version'
alias j17="export JAVA_HOME=`/usr/libexec/java_home -v 17`; java -version"
alias j21="export JAVA_HOME=$(/usr/libexec/java_home -v 21); java -version"
export JAVA_HOME=$(/usr/libexec/java_home -v 21)
# export JAVA_HOME="/Library/Java/JavaVirtualMachines/temurin-21.jdk/Contents/Home"
alias j="java"

# Python
#-------
# export PATH="/opt/homebrew/opt/python@3.13/libexec/bin:$PATH"
alias python="python3"
alias py="python3"

# Docker
#-------
alias d="docker"
alias dc="docker compose"

# Kubernetes
#-----------
alias k="kubectl"
alias kctx="kubectx"
alias kcx="kubectx"
alias kx="kubectx"
alias kns="kubens"
alias kgpo="kubectl get pods"
alias kgpon="kubectl get pods -n"
alias kgpow="kubectl get pods --watch"
alias klo="kubectl logs -f"
alias klog="kubectl logs -f"
alias klogs="kubectl logs -f"
alias kpo="kubectl get pods"
alias kpon="kubectl get pods -n"
alias kpow="kubectl get pods --watch"

# Environment Variables ENV
#--------------------------
# export EDITOR="codium --wait"
export EDITOR="code --wait"
export VISUAL="$EDITOR"
export LS_COLORS="$LS_COLORS:ow=1;34:tw=1;34:di=1;36" # Prevent highlighting of directories (green background)
export TZ="Australia/Sydney"

# VSCodium > VSCode
#--------------------
# alias code="codium"
# alias vscode="code"
# alias vcode="code"

# Custom Functions
# ----------------
# Find process ID of process using given port (usage: find-process-id-on-port 3000)
findport() {
  if [ $# -ne 1 ]; then
    echo "Usage: fport <port-number-to-search>"
    return 1
  fi
  lsof -i :$1
}
alias find-port="findport"
alias fp="findport"
alias fport="findport"
# Kill process ID of process using given port (usage: kill-process-on-port 3000)
killport() {
  if [ $# -ne 1 ]; then
    echo "Usage: kport <port-number-to-free>"
    return 1
  fi
  if [ -z "$(lsof -t -i:$1)" ]; then
    echo -e "NO process is using port: \033[1;32m$1\033[0m"
    return 1
  fi
  kill -9 $(lsof -t -i:$1)
}
# alias kpo="kport" # Note: This CONFLICTS with kpo == k get po
alias kill-port="killport"
alias kport="killport"
alias freep="killport"
alias freepo="killport"
alias freeport="killport"
# Kill given process id PID (usage: kp 1234)
killpid() {
  if [ $# -ne 1 ]; then
    echo "Usage: kpid <pid-to-kill>"
    return 1
  fi
  kill -9 $1
}
alias kill-pid="killpid"
alias kpi="killpid"
alias kpid="killpid"
# Show All Processes Owned By User (usage: mine)
mine() {
  ps "$@" -u "${USER}" -o pid,%cpu,%mem,start,command | sort -k 2 -r | awk '{ $5=substr($5,1,70); for(i=6;i<=NF;i++) $5=$5" "$i; $5=substr($5,1,70); NF=5; printf "%5s %4s %4s %7s %-70s\n", $1, $2, $3, $4, $5 }'
  # ps "$@" -u "${USER}" -o pid,%cpu,%mem,start,command | sort -k 2 -r | awk '{ $5=substr($5,1,70); for(i=6;i<=NF;i++) $5=$5" "$i; $5=substr($5,1,70); NF=5; print }'
  # ps "$@" -u "${USER}" -o pid,%cpu,%mem,start,time,bsdtime,command --sort=-%cpu # Note: --sort=-%cpu does NOT work on macOS
  # ps "$@" -u "${USER}" -o pid,%cpu,%mem,start,time,etime,command | sort -k 2 -r # Note: bsdtime does NOT work on macOS
  # ps "$@" -u "${USER}" -o pid,%cpu,%mem,start,time,etime,command | sort -k 2 -r | awk '{ $7=substr($7,1,70); for(i=8;i<=NF;i++) $7=$7" "$i; $7=substr($7,1,70); NF=7; print }'
  # ps "$@" -u "${USER}" -o pid,%cpu,%mem,start,time,etime,command | sort -k 2 -r | awk '{ $7=substr($7,1,70); for(i=8;i<=NF;i++) $7=$7" "$i; $7=substr($7,1,70); NF=7; print $1 " | " $2 " | " $3 " | " $4 " | " $5 " | " $6 " | " $7 }'
}
# Show Top 15 CPU Hogs (usage: cpuhogs)
cpuhogs() {
  ps "$@" -u "${USER}" -o pid,%cpu,%mem,start,command | sort -k 2 -r | head -15 | awk '{ $5=substr($5,1,70); for(i=6;i<=NF;i++) $5=$5" "$i; $5=substr($5,1,70); NF=5; printf "%5s %4s %4s %7s %-70s\n", $1, $2, $3, $4, $5 }'
}
# Show Top 15 Memory Hogs (usage: memhogs)
memhogs() {
  ps "$@" -u "${USER}" -o pid,%cpu,%mem,start,command | sort -k 3 -r | head -15 | awk '{ $5=substr($5,1,70); for(i=6;i<=NF;i++) $5=$5" "$i; $5=substr($5,1,70); NF=5; printf "%5s %4s %4s %7s %-70s\n", $1, $2, $3, $4, $5 }'
}
ps-aux-regex() {
  if [ $# -ne 1 ]; then
    echo "Usage: psx <process-name-to-find-using-regex>"
    return 1
  fi
  # ps ax -o user,pid,%cpu,%mem,start,command | grep -e "$1"
  ps ax -o user,pid,%cpu,%mem,start,command | sort -k 3 -r | grep -e "$1"
}
alias psaux="ps-aux-regex"
alias psx="ps-aux-regex"
alias psxre="ps-aux-regex"
alias psxreg="ps-aux-regex"
ps-aux-ignore-case() {
  if [ $# -ne 1 ]; then
    echo "Usage: psaux <process-name-to-find-ignore-case>"
    return 1
  fi
  # ps ax -o user,pid,%cpu,%mem,start,command | grep -i "$1"
  ps ax -o user,pid,%cpu,%mem,start,command | sort -k 3 -r | grep -i "$1"
}
alias psauxi="ps-aux-ignore-case"
alias psauxic="ps-aux-ignore-case"
alias psxi="ps-aux-ignore-case"
alias psxic="ps-aux-ignore-case"
ppkill() {
  if [ $# -ne 1 ]; then
    echo "Usage: ppkill <process-name-to-kill>"
    return 1
  fi
  pkill -9 -f "$1"
}
alias fkill="ppkill"
alias fkill!="ppkill"
alias fq="ppkill"
alias fq!="ppkill"
alias kill!="ppkill"
alias pkill!="ppkill"
alias quit="ppkill"
# Create directory and cd into it (usage: mkcd dirName)
mkcd() {
  if [ $# -ne 1 ]; then
    echo "Usage: mkcd <directory-name-to-create>"
    return 1
  fi
  if [ -d "$1" ]; then
    echo -en "Delete EXISTING folder \033[1;31m$1\033[0m? (y/N): "
    read -r response
    # if [[ ! "$response" =~ ^[yY]$ ]] && [[ ! "$response" =~ ^$ ]]; then
    # if [[ ! "$response" =~ ^[yY]$ ]] && [[ -n "$response" ]]; then
    if [[ ! "$response" =~ ^[yY]$ ]]; then
      echo -e "CANCELLED deleting \033[1;33m$1\033[0m (already exists)"
      return 1
    else
      echo -e "\033[1;31mrm -rf $1\033[0m && \033[1;36mmkdir -p $1\033[0m"
      rm -rf "$1"
    fi
  fi
  mkdir -p "$1"
  cd "$1"
  # mkdir -p "$@"
  # cd "$@"
}
# Open Project with IntelliJ (usage: idea .)
idea() {
  open -a "IntelliJ IDEA CE" "$1"
}
# OhMyZSH Override
git_main_branch() {
  echo master
}
# Upgrade Brew Casks
brew-upgrade-casks() {
  brew upgrade --cask $(brew list --cask)
  # brew upgrade --cask $(brew outdated --cask --greedy)
}
# Upgrade zsh plugins
zsh-upgrade-plugins() {
  omz update
  for dir in "$ZSH/custom/plugins"/*; do
    if [ -d "$dir/.git" ]; then
      pluginName=$(basename "$dir")
      echo -e "Updating: \033[1;36m$pluginName\033[0m"
      cd "$dir"
      git pull --rebase --autostash
      # cd - > /dev/null
    else
      echo -e "\033[1;33m$(basename "$dir")\033[0m is NOT a git repository, skipping"
    fi
  done
  cd
}
alias omz-upgrade-plugins="zsh-upgrade-plugins"
alias omz-update="zsh-upgrade-plugins"
alias omz-upgrade="zsh-upgrade-plugins"
alias zsh-update="zsh-upgrade-plugins"
alias zsh-upgrade="zsh-upgrade-plugins"
# Change Java JDK version (usage: jdk 17)
change-jdk() {
  if [ $# -ne 1 ]; then
    echo "Usage: jdk <java-jdk-version-to-use>"
    return 1
  fi
  echo -e "Switching to Java JDK \033[1;32mv$1\033[0m";
  export JAVA_HOME=$(/usr/libexec/java_home -v $1)
  echo "\$JAVA_HOME:" $JAVA_HOME
  echo "\$(java -version):"
  echo "------------------"
  java -version
  echo "------------------"
}
alias jdk="change-jdk"
java_compile_and_run() {
  if [ "$#" -eq 0 ]; then
    echo "Usage: jav <source_file>"
    return 1
  fi
  local srcFile="$1"
  # Append '.java' if not present
  if [[ "$srcFile" != *.java ]]; then
    srcFile="${srcFile}.java"
  fi
  local fileName="${srcFile%.*}"
  local outputFile="$fileName.class"
  javac "$srcFile"
  echo -e "Compiled \033[1;32m$srcFile\033[0m to \033[1;32m$outputFile\033[0m"
  echo -e "Running \033[1;32mjava $fileName\033[0m"
  java "$fileName"
}
alias j--="java_compile_and_run"
alias j++="java_compile_and_run"
alias jav="java_compile_and_run"
alias jc="java_compile_and_run"
alias jcr="java_compile_and_run"
g++_compile_and_run() {
  if [ "$#" -eq 0 ]; then
    echo "Usage: gpp <source_file>"
    return 1
  fi
  local srcFile="$1"
  # Append '.cpp' if not present
  if [[ "$srcFile" != *.cpp ]]; then
    srcFile="${srcFile}.cpp"
  fi
  local outputFile="${srcFile%.*}"
  g++-13 "$srcFile" -o "$outputFile"
  echo -e "Compiled \033[1;32m$srcFile\033[0m to \033[1;32m$outputFile\033[0m"
  echo -e "Running \033[1;32m./$outputFile\033[0m"
  ./"$outputFile"
}
alias c++="g++_compile_and_run"
alias cpp="g++_compile_and_run"
alias g--="g++_compile_and_run"
alias gpp="g++_compile_and_run"
# Delete both local and remote branch (usage: gbdd branchNameToDelete)
gbdd() {
  if [ $# -ne 1 ]; then
    echo "Usage: gbdd <branch-name-to-delete>"
    return 1
  fi
  local branchToDelete=$1
  if [ "$branchToDelete" = "master" ] || [ "$branchToDelete" = "main" ]; then
    echo -e "ERROR: Cannot delete \033[1;31m$branchToDelete\033[0m"
    return 1
  fi
  # local currBranch=$(git rev-parse --abbrev-ref HEAD)
  local currBranch=$(git_current_branch)
  local didGitStash=false
  if [ "$currBranch" = "$branchToDelete" ]; then
    echo "Error: You cannot delete the branch you are currently on"
    echo -e "Switching to \033[1;35m$(git_main_branch)\033[0m branch"
    git stash --include-untracked && didGitStash=true
    git checkout $(git_main_branch)
  fi
  git fetch --all --prune --tags --force > /dev/null 2>&1
  echo -en "Confirm DELETION of branch \033[1;31m$branchToDelete\033[0m in both LOCAL and REMOTE? [y/N]: "
  # echo -en "Confirm deletion of \033[1;31mlocal/$branchToDelete\033[0m && \033[1;31mremote/$branchToDelete\033[0m? [y/N]: "
  # echo -en "Confirm deletion of \033[1;31mlocal:$branchToDelete\033[0m && \033[1;31mremote:$branchToDelete\033[0m? [y/N]: "
  read -r response
  # if [[ ! "$response" =~ ^[yY]$ ]] && [[ ! "$response" =~ ^$ ]]; then
  # if [[ ! "$response" =~ ^[yY]$ ]] && [[ -n "$response" ]]; then
  if [[ ! "$response" =~ ^[yY]$ ]]; then
    echo -e "Branch deletion CANCELLED"
    return 1
  else
    # echo -e "Deleting \033[1;31mlocal/$branchToDelete\033[0m && \033[1;31mremote/$branchToDelete\033[0m"
    # echo -e "Deleting \033[1;31mlocal:$branchToDelete\033[0m && \033[1;31mremote:$branchToDelete\033[0m"
    # echo -e "Deleted branch \033[1;31m$branchToDelete\033[0m"
    echo -e "DELETED branch \033[1;31m$branchToDelete\033[0m in both LOCAL and REMOTE"
    git branch -D "$branchToDelete" # 2>/dev/null
    git push origin --delete "$branchToDelete"
  fi
  if [ "$didGitStash" = true ]; then
    git stash pop
  fi
}
alias gbDd="gbdd"
alias gbDD="gbdd"
# Delete remote branch (usage: gbdr remoteBranchNameToDelete)
gbdr() {
  if [ $# -ne 1 ]; then
    echo "Usage: gbdr <remote-branch-name-to-delete>"
    return 1
  fi
  local branchToDelete=$1
  if [ "$branchToDelete" = "master" ] || [ "$branchToDelete" = "main" ]; then
    echo -e "ERROR: Cannot delete \033[1;31m$branchToDelete\033[0m"
    return 1
  fi
  # local currBranch=$(git rev-parse --abbrev-ref HEAD)
  local currBranch=$(git_current_branch)
  git fetch --all --prune --tags --force > /dev/null 2>&1
  echo -en "Confirm DELETION of branch \033[1;31m$branchToDelete\033[0m in REMOTE? [y/N]: "
  # echo -en "Confirm deletion of \033[1;31mlocal/$branchToDelete\033[0m && \033[1;31mremote/$branchToDelete\033[0m? [y/N]: "
  # echo -en "Confirm deletion of \033[1;31mlocal:$branchToDelete\033[0m && \033[1;31mremote:$branchToDelete\033[0m? [y/N]: "
  read -r response
  # if [[ ! "$response" =~ ^[yY]$ ]] && [[ ! "$response" =~ ^$ ]]; then
  # if [[ ! "$response" =~ ^[yY]$ ]] && [[ -n "$response" ]]; then
  if [[ ! "$response" =~ ^[yY]$ ]]; then
    echo -e "Branch deletion CANCELLED"
    return 1
  else
    # echo -e "Deleting \033[1;31mlocal/$branchToDelete\033[0m && \033[1;31mremote/$branchToDelete\033[0m"
    # echo -e "Deleting \033[1;31mlocal:$branchToDelete\033[0m && \033[1;31mremote:$branchToDelete\033[0m"
    # echo -e "Deleted branch \033[1;31m'$branchToDelete'\033[0m in both LOCAL and REMOTE"
    echo -e "Deleted \033[1;31mREMOTE\033[0m branch \033[1;31m$branchToDelete\033[0m"
    # git push origin --delete "$branchToDelete"
    git push origin :"$branchToDelete"
  fi
}
alias gbDr="gbdr"
# gcb newBranch && gco currBranch (used for git worktree workflow) [usage: gcbb newBranch]
gcbb() {
  if [ $# -ne 1 ]; then
    echo "Usage: gcbb <new-branch-name>"
    return 1
  fi
  local newBranch=$1
  if [ "$newBranch" = "$(git_main_branch)" ]; then
    echo -e "ERROR: Cannot create branch \033[1;31m$newBranch\033[0m; it is the default branch"
    return 1
  fi
  # local currBranch=$(git rev-parse --abbrev-ref HEAD)
  local currBranch=$(git_current_branch)
  if [ "$currBranch" = "$newBranch" ] || git show-ref --verify --quiet refs/heads/"$newBranch"; then
    echo -e "ERROR: Branch \033[1;31m$newBranch\033[0m already exists"
    return 1
  fi
  echo -e "Created branch \033[1;32m$newBranch\033[0m && switching back to branch \033[1;35m$currBranch\033[0m"
  git checkout -b "$newBranch" && git checkout "$currBranch"
}
alias gcbo="gcbb"
gundo() {
  if [ $# -ne 1 ]; then
    echo "Usage: gundo <number-of-commits-to-rewind>"
    return 1
  fi
  if ! [[ "$1" =~ ^[1-9][0-9]*$ ]]; then
    echo -e "\033[1;31mERROR: Input must a number > 0\033[0m"
    return 1
  fi
  # totalNumCommits=$(git rev-list --count HEAD)
  # if [ "$1" -gt "$totalNumCommits" ]; then
  #   echo -e "ERROR: Cannot undo \033[1;31m$1\033[0m commits (only \033[1;32m$totalNumCommits\033[0m commits exist)"
  #   return 1
  # fi
  git reset --mixed HEAD~$1
}
# Either a git repository (.git folder) or git worktree repository (.git file && .gitignore file) [usage: is-git-repo; echo $?]
is-git-repo() {
  # V1
  [ -d ".git" ] || { [ -f ".git" ] && [ -f ".gitignore" ]; } && return 0 || return 1
  # [ -d ".git" ] || ([ -f ".git" ] && [ -f ".gitignore" ]) && return 0 || return 1
  # V2
  # [ -d ".git" ] || { [ -f ".git" ] && [ -f ".gitignore" ]; }
  # [ -d ".git" ] || ([ -f ".git" ] && [ -f ".gitignore" ])
  # return $?
}
is-not-git-repo() {
  ! { [ -d ".git" ] || { [ -f ".git" ] && [ -f ".gitignore" ]; } } && return 0 || return 1
  # ! ( [ -d ".git" ] || ([ -f ".git" ] && [ -f ".gitignore" ]) ) && return 0 || return 1
}
safe-git-stash-pop() {
  # if [ $# -ne 1 ] && ! { [ -d ".git" ] || { [ -f ".git" ] && [ -f ".gitignore" ]; } }; then
  # if [ $# -ne 1 ] && ! ( [ -d ".git" ] || ([ -f ".git" ] && [ -f ".gitignore" ]) ); then
  # if [ $# -ne 1 ] && ! is-git-repo; then
  if [ $# -ne 1 ] && ! [ -d ".git" ]; then
    echo -e "ERROR: \033[1;31m$(basename $(pwd))\033[0m is NOT a git repository, skipping"
    return 1
  fi
  # Try to apply stash without committing it
  local dir=${1:-$(basename $(pwd))}
  if git stash show -p | git apply --check 2>/dev/null; then
    echo -e "\033[1;33mPopping from stash:\033[0m"
    for file in $(git stash show --name-only); do
      # echo -e "\e[1;31m- Modified: $file\e[0m"
      echo -e "\033[1;31m- Modified: $file\033[0m"
    done
    git stash pop > /dev/null 2>&1
  else
    echo -e "\033[1;33mWARNING: Stash conflicts detected in\033[0m \033[1;31m$dir\033[0m (use 'git stash show -p' to see changes && use 'git stash pop' when ready to merge)"
  fi
}
alias can-gstp="safe-git-stash-pop"
update-git-repos() {
  local startDir=$(pwd)
  for dir in */; do
    if [ -d "$dir" ]; then
      # Strip trailing slash
      dir=${dir%/}
      cd "$dir" || continue
      # if [ -d ".git" ] || ([ -f ".git" ] && [ -f ".gitignore" ]); then
      # if [ -d ".git" ] || { [ -f ".git" ] && [ -f ".gitignore" ]; }; then
      # if is-git-repo; then
      if [ -d ".git" ]; then
        # echo -e "Updating: \e[1;32m$dir\e[0m"
        echo -e "Updating: \033[1;36m$dir\033[0m"
        if [ -n "$(git status --porcelain)" ]; then
          git stash --include-untracked > /dev/null 2>&1
        fi
        # git fetch --all --prune --tags --force && git pull --rebase --autostash
        # git fetch --all --prune --tags --force > /dev/null 2>&1 && git pull --rebase --autostash > /dev/null 2>&1
        git fetch --all --prune --tags --force > /dev/null 2>&1 && git pull --rebase --autostash
        if [ $(git stash list | wc -l) -gt 0 ]; then
          safe-git-stash-pop "$dir"
        fi
      else
        echo -e "\033[1;33m$dir\033[0m is NOT a git repository, skipping"
      fi
      cd "$startDir"
      echo ""
    fi
  done
}
alias gl-repos="update-git-repos"

# Git Aliases
#------------
alias gbd!='git branch -D'
alias gclean='git reset --hard && git clean -dfx'
alias gcleano='git fetch --all --prune --tags --force && git reset --hard origin/$(git_current_branch) && git clean -dfx'
alias gcmsg!='git commit --amend -m'
alias gf='git fetch --all --prune'
alias gfa='git fetch --all --prune'
alias gf!='git fetch --all --prune --tags --force'
alias gl="git pull --rebase --autostash"
alias glo="git log --pretty='%C(yellow)%h %C(cyan)%cd %C(red)%aN %Creset%s%C(green)%d' --date=format:'%d/%m/%y %I:%M%p'"
alias glo2="git log --graph --oneline --decorate --pretty='%C(yellow)%h %C(cyan)%cd %C(red)%aN %Creset%s%C(green)%d' --date=format:'%d/%m/%y %I:%M%p'"
alias glo3="git log --pretty='%C(yellow)%h %C(cyan)%cd %C(magenta)%aN %Creset%s%C(green)%d' --date=format:'%d/%m/%y %I:%M:%S%p'"
alias gpf="git push --force-with-lease --force-if-includes"
alias gpf!="git push --force-with-lease --force-if-includes"
alias gpF!="git push --force"
alias gpf@="git push --force"
alias gpff="git push --force"
alias gstash='git stash --include-untracked'
alias gstashp='git stash pop'
alias gsth='git stash --include-untracked'
alias gstp='git stash pop'
alias gstsh='git stash --include-untracked'
alias gstshp='git stash pop'
alias gundo1='git reset --mixed HEAD~1'
alias gundo!='git reset --hard ORIG_HEAD' # Undo's the gundo (note: ORIG_HEAD is a reference that points to the previous state of HEAD before disruptive operations like git reset, git rebase, git merge)
alias gwta='git worktree add' # cd myRepo && gwta ../targetBranchDir targetBranch
alias gwtl='git worktree list' # (cd myRepo || cd targetBranchDir) && gwtl
alias gwtr='git worktree remove' # (cd myRepo && gwtr ../targetBranchDir) || (cd targetBranchDir && gwtr .)

# cd Aliases
#-----------
alias b="cd .."
alias cdd="cd ~/dev/github"
alias cd1="cd ~/dev/github"
alias cd2="cd ~/dev/github28"

# Aliases
#--------
# alias cpuhogs='ps wwaxr -o pid,stat,%cpu,%mem,time,command | head -10' # Show Top 10 CPU Hogs
# alias gcp='(){ ga . && gc -m $1 && gp; }'
# alias kill='kill -9'
# alias ll='ls -AlF --color=auto'
# alias memhogs='ps wwaxm -o pid,stat,vsize,rss,time,command | head -10' # Show Top 10 Memory Hogs
alias brew-outdated-cask="brew update > /dev/null 2>&1 && brew outdated --cask --greedy"
alias brew-outdated="brew update > /dev/null 2>&1 && brew outdated --greedy"
alias brew-update="brew update > /dev/null 2>&1 && brew outdated --greedy"
alias cls="clear"
alias cude="codium"
alias egrep="egrep --color=always"
alias finder="open"
alias fgrep="fgrep --color=always"
alias g++="g++-13"
alias gcc="gcc-13"
alias grep='grep --color=always'
alias ll='ls -AoF --color=auto'
alias ls='ls --color=auto'
alias lsd='ls -d */'
alias rezsh='source ~/.zshrc'
alias untar="tar -xvf"
