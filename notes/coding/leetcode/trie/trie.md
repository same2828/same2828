# Table of Contents

- [Table of Contents](#table-of-contents)
- [What is a Trie](#what-is-a-trie)

# What is a Trie

- A Trie is a special form of a Nary tree
- Typically, a trie is used to store strings
- Each `Trie node` represents a string (a prefix)
- Each node might have several children nodes while the paths to different children nodes represent different characters
- And the strings the child nodes represent will be the `origin string` represented by the `node itself + the character on the path`
